<%= form_for @location, html: {id: :location_form } do |f| %>
  <%#wrap in fields with errors class %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>

  <%= f.label :address %>
  <%= f.text_field :address %>

  <%= f.submit%>

<% end %>