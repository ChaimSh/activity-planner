<%= form_for @location do |f| %>
  <%#wrap in fields with errors class %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>

  <%= f.label :address %>
  <%= f.text_field :address %>

  <%= f.submit%>

<% end %>